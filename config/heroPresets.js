// Creative scene choices stay separate from reusable engine defaults.
export const HERO_PRESETS = {
  precisionField: {
    id: "precision-field",
    frameTypes: [],
    distribution: {},
  },
};

export const DEFAULT_HERO_PRESET = HERO_PRESETS.precisionField;
