// Future registry for extensible frame shapes and their drawing strategies.
export default class FrameTypeRegistry {
  constructor(options = {}) {
    this.options = options;
  }

  init() {}

  destroy() {}
}
